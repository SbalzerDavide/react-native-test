import { Text } from "@/components/ui/text";
import { Box } from "@/components/ui/box";

const AboutScreen = () => {
  return (
    <Box className="flex-1 justify-center items-center">
      <Text className="text-typography-900 text-2xl">About</Text>
    </Box>
  );
};

export default AboutScreen;
